{
  // Enable `deno` tooling
  "deno.enable": true,
  "deno.lint": true,
  "deno.unstable": true,
  "deno.codeLens.testArgs": [
    "--allow-all"
  ],
  // Format-on-Save using Deno
  "editor.defaultFormatter": "denoland.vscode-deno",

  // Turn off the `tsserver` integration
  "typescript.validate.enable": false
}
